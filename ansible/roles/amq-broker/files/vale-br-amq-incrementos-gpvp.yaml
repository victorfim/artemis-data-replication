kind: ActiveMQArtemisAddress
metadata:
  name: vale.br.amq.incrementos.gpvp
spec:
  addressName: vale.br.amq.incrementos.gpvp
  queueName: vale.br.amq.incrementos.gpvp
  routingType: anycast
  removeFromBrokerOnDelete: true
